pipeline {
    agent any
    
    environment {
    		all = "${PYTHON} ${BASH} ${C}"
		python = python.py
		bash = bash
		c = c
	}
    
        stages {
            stage('Build') {   steps {   sh 'echo $all'   }   }   
        
}
