pipeline {
   agent any

   stages {
      stage('Build') {
         steps {
            echo 'The Build is started'
         }
      }
	  stage('Test') {
         steps {
            echo 'The Test is started'
         }
      }
	  stage('Deploy') {
         steps {
            echo 'The Deploy is started'
         }
      }
   }
}
