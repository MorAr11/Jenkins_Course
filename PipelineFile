pipeline {
   agent {label 'slave01'}
   environment {
	ALL = 'all'
	BASH = 'bash'
	PYTHON = 'python'
	C = 'c'
	
   }

   stages {
      stage('python') {
         steps {
            sh 'cat $LANGUAGE'
         }
      }
	  stage('bash') {
         steps {
            sh 'cat $LANGUAGE'
         }
      }
	  stage('c') {
         steps {
            sh 'cat $LANGUAGE'
         }
      }
   }
}
